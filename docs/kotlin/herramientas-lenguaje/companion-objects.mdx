---
sidebar_position: 4
title: Companion objects
description: Una forma interesante que tiene Kotlin para poner métodos y atributos en las clases.
---

import { VideoYouTube } from '../../../src/components/VideoYouTube';

Si venís de un lenguaje como Java o Python, probablemente hayas utilizado alguna vez los métodos o atributos de clase: `static` en Java o `@classmethod` en Python.

En este video te presentamos los **companion objects**, el mecanismo que ofrece Kotlin para lograr un efecto similar.

<VideoYouTube
  id="2RrkG0nCrBw"
  titulo="Companion objects en Kotlin, o cómo poner código en las clases"
/>
